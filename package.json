{
  "name": "triveni-gst",
  "private": true,
  "version": "3.0.0",
  "description": "TriVista GST ERP — Express + MySQL backend that also serves the built React (Vite) frontend. Single Railway service.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node src/server.js"
  }
}
</content>